# 校园资源预约平台 —— 数据库设计文档

> 数据库：MySQL 8.x　　编码：`utf8mb4_unicode_ci`
> 库名：`campus_reservation`　　初始化脚本：`src/main/resources/db/schema.sql`

---

## 1. 设计约定

### 1.1 通用约定

| 约定 | 说明 |
| ---- | ---- |
| 主键 | `id BIGINT AUTO_INCREMENT` |
| 逻辑删除 | 通用字段 `deleted TINYINT(0/1)`，由 MyBatis-Plus `@TableLogic` 处理 |
| 时间字段 | `created_at` / `updated_at`，自动填充 |
| 枚举 | 统一以整型存 TinyInt，含义见各表注释 |

### 1.2 预约时间区间约定

预约时间段统一采用**半开区间 `[start, end)`** 存储：前一个预约的 `end` 可与后一个预约的 `start` 相等而不被判为冲突，从而正确区分"连续预约"与"时间冲突"。

重叠判定条件：

```text
冲突 ⟺  newStart < existEnd  AND  newEnd > existStart
```

---

## 2. 实体关系概览（ER）

```
sys_user 1 ──── N reservation ──── N 1 resource
                    │  1                   1 │
                    │                        │
                    N                        N
                violation               course_schedule
                                      repair_order
sys_user 1 ──── N violation
sys_user 1 ──── N repair_order
resource 1 ──── N course_schedule
resource 1 ──── N checkin_code
resource 1 ──── N repair_order
```

- 系统维护节假日 `holiday`，预约创建时校验日期是否命中节假日区间。

---

## 3. 表结构明细

### 3.1 `sys_user` 用户表

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | BIGINT | PK, 自增 | 主键 |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 登录名 |
| password | VARCHAR(128) | NOT NULL | 密码（BCrypt 加密）|
| nickname | VARCHAR(50) | | 姓名/昵称 |
| role | TINYINT | NOT NULL, 默认 0 | 角色：0 用户、1 管理员 |
| phone | VARCHAR(20) | | 手机号 |
| email | VARCHAR(100) | | 邮箱 |
| status | TINYINT | NOT NULL, 默认 1 | 状态：1 正常、0 禁用 |
| created_at | DATETIME | 默认 CURRENT_TIMESTAMP | 创建时间 |
| deleted | TINYINT | 默认 0 | 逻辑删除 |

索引：`uk_username`（username 唯一）

---

### 3.2 `resource` 资源表

统一存放四类资源：教室/实验室/图书馆/体育馆，用 `resource_type` 区分。

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | BIGINT | PK, 自增 | 主键 |
| name | VARCHAR(100) | NOT NULL | 资源名称 |
| resource_type | TINYINT | NOT NULL | 类型：1 教室、2 实验室、3 图书馆、4 体育馆 |
| building | VARCHAR(50) | | 所在楼宇 |
| floor | VARCHAR(20) | | 楼层/位置 |
| room_no | VARCHAR(50) | | 房间号/场地号 |
| capacity | INT | 默认 0 | 容纳人数 |
| longitude | DECIMAL(10,6) | | 经度（位置服务/定位打卡）|
| latitude | DECIMAL(10,6) | | 纬度 |
| open_time | TIME | NOT NULL, 默认 08:00 | 开放时间 |
| close_time | TIME | NOT NULL, 默认 22:00 | 关闭时间 |
| max_duration | INT | NOT NULL, 默认 120 | 单次预约时长上限（分钟）|
| status | TINYINT | NOT NULL, 默认 1 | 状态：1 待使用、2 正在被使用、3 维修中 |
| description | VARCHAR(500) | | 描述 |
| created_at / updated_at / deleted | | | 通用字段 |

索引：`idx_type_status`（resource_type, status）

---

### 3.3 `reservation` 预约表（核心表）

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | BIGINT | PK, 自增 | 主键 |
| user_id | BIGINT | NOT NULL | 预约用户 ID（FK → sys_user）|
| resource_id | BIGINT | NOT NULL | 资源 ID（FK → resource）|
| start_time | DATETIME | NOT NULL | 开始时间（半开区间左端）|
| end_time | DATETIME | NOT NULL | 结束时间（半开区间右端）|
| checkin_type | TINYINT | | 打卡方式：1 定位、2 验证码 |
| checkin_flag | TINYINT | NOT NULL, 默认 0 | 是否已打卡：0 否、1 是 |
| checkin_time | DATETIME | | 打卡时间 |
| remark | VARCHAR(255) | | 备注 |
| created_at / updated_at / deleted | | | 通用字段 |

索引：`idx_user`（user_id）、`idx_resource_time`（resource_id, start_time, end_time）

> `idx_resource_time` 供时间冲突检测使用：按 `(resource_id, start_time, end_time)` 定位同一资源的重叠记录。

---

### 3.4 `course_schedule` 课表记录表（教室模块）

记录课程占用的时间，用于"排除课程占用 → 计算空闲教室"。

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | BIGINT | PK, 自增 | 主键 |
| room_id | BIGINT | NOT NULL | 教室（resource.id）|
| course_name | VARCHAR(100) | NOT NULL | 课程名称 |
| day_of_week | TINYINT | NOT NULL | 星期：1~7 |
| start_time | TIME | NOT NULL | 上课时间 |
| end_time | TIME | NOT NULL | 下课时间 |
| teacher | VARCHAR(50) | | 教师 |
| term | VARCHAR(20) | | 学期 |
| created_at / updated_at / deleted | | | 通用字段 |

索引：`idx_room_day`（room_id, day_of_week）

---

### 3.5 `checkin_code` 打卡验证码表

文字打卡的动态验证码，管理员在资源现场设置。

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | BIGINT | PK, 自增 | 主键 |
| resource_id | BIGINT | NOT NULL | 资源 ID |
| code | VARCHAR(20) | NOT NULL | 动态验证码 |
| valid_date | DATE | NOT NULL | 有效日期 |
| used | TINYINT | NOT NULL, 默认 0 | 是否使用：0 否、1 是 |
| expires_at | DATETIME | NOT NULL | 过期时间 |
| created_at | DATETIME | 默认 CURRENT_TIMESTAMP | 创建时间 |

索引：`idx_res_date`（resource_id, valid_date）

---

### 3.6 `violation` 违规记录表

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | BIGINT | PK, 自增 | 主键 |
| user_id | BIGINT | NOT NULL | 违规用户（FK → sys_user）|
| reservation_id | BIGINT | | 关联预约 ID |
| type | TINYINT | NOT NULL | 类型：1 爽约、2 超时使用、3 恶意取消、4 其他 |
| description | VARCHAR(500) | | 违规描述 |
| deduct_score | INT | NOT NULL, 默认 0 | 扣除信用分 |
| status | TINYINT | NOT NULL, 默认 1 | 状态：1 有效、0 已撤销 |
| created_at | DATETIME | 默认 CURRENT_TIMESTAMP | 创建时间 |

索引：`idx_user`（user_id）

> 业务联动：登记违规后同步扣减 `sys_user.credit_score`；信用低于阈值时限制预约。

---

### 3.7 `repair_order` 维修工单表

用户报修 → 管理员处理 → 暂停资源 → 维修完成 → 恢复使用的完整闭环。

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | BIGINT | PK, 自增 | 主键 |
| resource_id | BIGINT | NOT NULL | 报修资源（FK → resource）|
| user_id | BIGINT | NOT NULL | 报修人（FK → sys_user）|
| problem | VARCHAR(500) | NOT NULL | 问题描述 |
| image_url | VARCHAR(255) | | 图片 |
| status | TINYINT | NOT NULL, 默认 0 | 状态：0 待处理、1 处理中、2 已完成、3 已驳回 |
| handle_user | BIGINT | | 处理人 ID |
| handle_time | DATETIME | | 处理时间 |
| created_at / updated_at / deleted | | | 通用字段 |

索引：`idx_resource`（resource_id）、`idx_user`（user_id）

---

### 3.8 `holiday` 节假日表

系统自动阻止节假日期内创建预约。

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | BIGINT | PK, 自增 | 主键 |
| name | VARCHAR(50) | NOT NULL | 节假日名称 |
| start_date | DATE | NOT NULL | 开始日期 |
| end_date | DATE | NOT NULL | 结束日期 |
| created_at | DATETIME | 默认 CURRENT_TIMESTAMP | 创建时间 |

索引：`idx_dates`（start_date, end_date）

---

## 4. 核心业务表关联与调用链

| 业务 | 数据流 |
| ---- | ---- |
| 预约 | `resource`（可用性）→ `reservation` 冲突检测 → 创建预约 → `checkin_code`/定位打卡 |
| 空闲教室 | `course_schedule`（占用）→ 排除 → `resource(type=1)` 空闲时段 |
| 违规 | `reservation`（未打卡/超时）→ `violation` → 扣减 `sys_user.credit_score` |
| 维修 | `resource` 状态 → `repair_order`（报修→处理→完成）→ 恢复 `resource.status` |
| 节假日 | `holiday` 区间校验 → 拒绝 `reservation` 创建 |

---

## 5. 补充说明

- 以上为**逻辑外键**设计：表间关联通过业务字段维护，未建立物理外键约束，便于扩容与性能。
- 定位打卡阈值、信用分门槛等属于"规则配置"，由各服务模块定义，未单独建表。
- 如需新建业务表，请同时更新 `schema.sql` 与本文档。